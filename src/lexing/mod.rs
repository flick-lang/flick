/// Module that defines the [Lexer] struct for converting code to tokens.
pub mod lexer;

/// Module that defines the [Token](token::Token) enum.
pub mod token;

/// Module that defines the lexer errors.
pub mod error;
