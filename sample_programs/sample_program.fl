fn main() {
    i64 N = 5

    // todo: introduce inferred ('auto') types
    i64 answer = 1
    // todo: parse negative numbers
    i64 i = 1

    while i <= N {
        answer = answer * i
        i += 1
    }

    // todo: move print to std
    print(answer)
}