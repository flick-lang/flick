// Currently everything is wrapped with main() {...}

int N = 5

// todo: introduce const/let
// todo: introduce implicit types
int answer = 1
// todo: parse negative numbers
int i = 1

while i <= N {
    answer = answer * i
    i += 1
}

// todo: move print to std
print(answer)
