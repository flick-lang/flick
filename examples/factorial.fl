pub fn main() u8 {
    u8 N = 5

    // todo: introduce inferred ('auto') types
    u8 answer = 1
    u8 i = 1

    while i <= N {
        answer = answer * i
        i += 1
    }

    ret answer

    // todo: move print to std
    //print(answer)
}